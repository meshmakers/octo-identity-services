"$schema": "https://schemas.meshmakers.cloud/construction-kit-elements.schema.json"
types:
  - typeId: Permission
    derivedFromCkTypeId: ${System}/Entity
    isFinal: true
    isAbstract: false
    attributes:
      - id: ${this}/PermissionId
        name: PermissionId

      - id: ${this}/IdentityRoleIds
        name: IdentityRoleIds
